# Limoxel SDK Starter Template

A minimal, ready-to-run Go application template demonstrating how to embed and consume the Limoxel SDK.

## Quick Start

```bash
# Run the starter application
go run main.go
```

## Structure

- `main.go`: Minimal application entrypoint initializing the SDK client, opening the workspace, and running queries.
